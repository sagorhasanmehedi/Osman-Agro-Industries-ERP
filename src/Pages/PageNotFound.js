import React from "react";

const PageNotFound = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h5 style={{ margin: "auto" }}>The page not found</h5>
    </div>
  );
};

export default PageNotFound;
